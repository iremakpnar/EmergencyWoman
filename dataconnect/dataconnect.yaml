specVersion: "v1beta"
serviceId: "emergencywoman"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "emergencywoman-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
